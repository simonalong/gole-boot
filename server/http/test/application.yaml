gole:
  application:
    # 应用名称
    name: sample
  server:
    http:
      # 是否启用，默认：true
      enable: true
      # 端口号
      port: 8080
  logger:
    level: debug
    color:
      # 启用：true/false，默认：true
      enable: true
