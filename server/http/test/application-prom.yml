gole:
  application:
    # 应用名称
    name: sample
  server:
    http:
      # 是否启用，默认：true
      enable: true
      # 端口号
      port: 8082
      api:
        prefix: api
  prometheus:
    enable: true
