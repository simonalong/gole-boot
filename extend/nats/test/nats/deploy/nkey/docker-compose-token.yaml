services:
  nats-nkey-server:
    container_name: nats-nkey-server
    image: nats:2.10.16
    entrypoint: /nats-server
    command: "--config /config/nats.conf"
    ports:
      - 4222:4222
    volumes:
      - ./config:/config
