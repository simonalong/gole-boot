gole:
  application:
    name: test-nats
  nats:
    enable: true
    # nats的url，可以为一个，集群情况下，可以填写多个（多个之间英文逗号区分）；也可以不填，默认：nats://127.0.0.1:4222
    url: nats://192.168.1.83:31011
    user-name: admin
    password: admin@1234
    # 认证方式：密码，这个密码一定要用明文（服务端加密的话，代码中要用明文）
    jetstream:
      enable: true

